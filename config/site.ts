export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Auth Boilerplate",
  description:
    "A boilerplate for building authentication systems with Next.js and Lucia Auth.",
};
